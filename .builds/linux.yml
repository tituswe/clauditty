image: archlinux

packages:
  - pkg-config
  - cmake
  - make
  - freetype2
  - fontconfig
  - libxcb
  - libxkbcommon
  - scdoc

sources:
  - https://github.com/tituswe/clauditty

environment:
  PATH: /home/build/.cargo/bin:/usr/bin/

tasks:
  - rustup: |
      curl https://sh.rustup.rs -sSf | sh -s -- -y --default-toolchain stable --profile minimal
  - rustfmt: |
      cd clauditty
      rustup toolchain install nightly -c rustfmt
      cargo +nightly fmt -- --check
  - man-pages: |
      cd clauditty
      cat extra/man/clauditty.1.scd | scdoc > /dev/null
      cat extra/man/clauditty-msg.1.scd | scdoc > /dev/null
      cat extra/man/clauditty.5.scd | scdoc > /dev/null
      cat extra/man/clauditty-bindings.5.scd | scdoc > /dev/null
  - test: |
      cd clauditty
      cargo test
  - oldstable: |
      cd clauditty
      oldstable=$(cat Cargo.toml | grep "rust-version" | sed 's/.*"\(.*\)".*/\1/')
      rustup toolchain install --profile minimal $oldstable
      rustup default $oldstable
      cargo test
  - clippy: |
      cd clauditty
      rustup component add clippy
      cargo clippy --all-targets
  - feature-wayland: |
      cd clauditty/clauditty
      RUSTFLAGS="-D warnings" cargo test --no-default-features --features=wayland
  - feature-x11: |
      cd clauditty/clauditty
      RUSTFLAGS="-D warnings" cargo test --no-default-features --features=x11
